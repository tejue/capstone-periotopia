import styled from "styled-components";

export const StyledAverage = styled.div`
  display: inline-block;
  width: 25px;
  height: 25px;
  border: 0.5px solid var(--primary-highlight-color);
  border-radius: 5px;
  line-height: 25px;
  text-align: center;
  font-size: 20px;
  font-weight: 900;
  color: var(--primary-highlight-color);
  background-color: rgba(238, 237, 237, 0.3);
  cursor: pointer;
  position: absolute;
  bottom: 30px;
  right: 90px;
`;
