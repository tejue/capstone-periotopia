import styled from "styled-components";

export const StyledPeriotopiaInfo = styled.section`
  background-color: var(--primary-color);
  color: var(--primary-color-text);
  padding: 15px;
  margin: 10px 10px;
  border-radius: 5px;
  font-weight: 300;
  line-height: 1.3;
`;
